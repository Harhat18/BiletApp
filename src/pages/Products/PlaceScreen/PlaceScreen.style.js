import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {flex: 1},
  placeFilter: {
    textTransform: 'uppercase',
    textAlign: 'center',
    fontSize: 18,
    fontWeight: 'bold',
    margin: 5,
  },
});
