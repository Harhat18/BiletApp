import {View, Text, Button} from 'react-native';
import React from 'react';

const Profile = () => {
  return (
    <View style={{flex: 1}}>
      <Text> profile </Text>
    </View>
  );
};

export default Profile;
